import { submitQuery } from './contact.js';

document.querySelector ('.js-cv-button')
    .addEventListener ('click', () => {
        alert ('Nothing to see yet');
    });

document.querySelector ('.js-explore-button')
    .addEventListener ('click', () => {
        alert ('Nothing to see here yet');
    });

submitQuery ();